# prisma.legacy

Historical **PostgreSQL / Prisma** schema from upstream Spliit and the pre-D1 KV fork.

This directory is **not executed**. The live database is **Turso** (`migrations/`, `src/lib/db/`). Do not run `prisma migrate` or restore `@prisma/client`.
